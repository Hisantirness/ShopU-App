# Copia este archivo a `.env` y completa los valores si quieres ejecutar análisis localmente.
# Nota: En GitHub Actions usa Secrets, no subas `.env` al repo (ya está ignorado en .gitignore).

# SonarCloud
SONAR_TOKEN=
SONAR_ORGANIZATION=YOUR_SONARCLOUD_ORG
SONAR_HOST_URL=https://sonarcloud.io
